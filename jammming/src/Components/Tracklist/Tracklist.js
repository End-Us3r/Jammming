import React from 'react';

function Tracklist () {
    return (
        <dvi className='Tracklist'>
            {/* You will add a map method that renders a set of Track compontents */}
        </dvi>
    );
}

export default Tracklist;